export enum AvTodoState {
    Waiting,
    InProgress,
    Done
}
export class AvTodoData implements IData {
    public id: number = 0;
    public name: string = "";
    public state: AvTodoState = AvTodoState.Waiting;
}